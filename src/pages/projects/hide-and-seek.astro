---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { Image, getImage } from "astro:assets";
import screenshot1 from "../../assets/screenshots/hide-and-seek-1.png";
import screenshot2 from "../../assets/screenshots/hide-and-seek-2.png";
const base = import.meta.env.BASE_URL;
const poster1 = await getImage({ src: screenshot1, format: "webp", width: 800 });

const pageTitle = "Hide & Seek — Marko Stankovic";
const pageDescription =
  "A Unity 6 stealth game where enemy suspicion builds continuously — six NavMesh AI states, a generic StateMachine, and nine custom Level Builder editor tools.";
---

<BaseLayout title={pageTitle} description={pageDescription} codeHighlighting={true}>

  <Fragment slot="head">
    <script type="application/ld+json" is:inline>
    {
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Hide & Seek",
      "description": "Unity 6 stealth game with NavMesh AI, a six-state generic StateMachine, continuous suspicion tracking, and nine custom Level Builder editor tools.",
      "applicationCategory": "Game",
      "applicationSubCategory": "Stealth",
      "operatingSystem": "Windows",
      "author": { "@type": "Person", "name": "Marko Stankovic", "url": "https://markostankovic.org" },
      "url": "https://markostankovic.org/projects/hide-and-seek"
    }
    </script>
  </Fragment>
    <!-- ─── NAV ─────────────────────────────────────────────────────────── -->
    <header
      class="fixed top-0 inset-x-0 z-50 border-b border-[#1F1F1F]/60 bg-[#0D0D0D]/80 backdrop-blur-md"
    >
      <nav
        class="max-w-5xl mx-auto px-6 h-14 flex items-center justify-between"
      >
        <div class="flex items-center gap-5">
          <a
            href="/"
            class="nav-link text-sm text-[#888888] hover:text-[#00FFC8] transition-colors duration-150 flex items-center gap-1.5"
          >
            <svg
              class="w-3.5 h-3.5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 19l-7-7 7-7"></path>
            </svg>
            Back to Portfolio
          </a>
          <span class="text-[#1F1F1F]">|</span>
          <a
            href="/"
            class="font-bold font-mono tracking-tight text-[#F0F0F0] text-lg"
            >MS</a
          >
        </div>

        <ul class="hidden sm:flex gap-6 text-sm text-[#888888]">
          <li>
            <a
              href="/#skills"
              class="nav-link hover:text-[#F0F0F0] transition-colors duration-150"
              >Skills</a
            >
          </li>
          <li>
            <a
              href="/#projects"
              class="nav-link hover:text-[#F0F0F0] transition-colors duration-150"
              >Projects</a
            >
          </li>
          <li>
            <a
              href="/devlog"
              class="nav-link hover:text-[#F0F0F0] transition-colors duration-150"
              >Devlog</a
            >
          </li>
          <li>
            <a
              href="/#contact"
              class="nav-link hover:text-[#F0F0F0] transition-colors duration-150"
              >Contact</a
            >
          </li>
        </ul>

        <button
          id="menu-btn"
          class="sm:hidden w-11 h-11 flex items-center justify-center text-[#888888] hover:text-[#F0F0F0] transition-colors duration-150"
          aria-label="Toggle menu"
          aria-expanded="false"
        >
          <svg
            id="icon-menu"
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
          <svg
            id="icon-close"
            class="w-5 h-5 hidden"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </nav>

      <div id="mobile-nav" aria-hidden="true">
        <div class="max-w-5xl mx-auto px-6 py-2 flex flex-col">
          <a
            href="/"
            tabindex="-1"
            class="mobile-nav-link py-3 text-sm text-[#888888] hover:text-[#00FFC8] border-b border-[#1F1F1F] transition-colors duration-150 min-h-[44px] flex items-center"
            >← Back to Portfolio</a
          >
          <a
            href="/#skills"
            tabindex="-1"
            class="mobile-nav-link py-3 text-sm text-[#888888] hover:text-[#F0F0F0] border-b border-[#1F1F1F] transition-colors duration-150 min-h-[44px] flex items-center"
            >Skills</a
          >
          <a
            href="/#projects"
            tabindex="-1"
            class="mobile-nav-link py-3 text-sm text-[#888888] hover:text-[#F0F0F0] border-b border-[#1F1F1F] transition-colors duration-150 min-h-[44px] flex items-center"
            >Projects</a
          >
          <a
            href="/devlog"
            tabindex="-1"
            class="mobile-nav-link py-3 text-sm text-[#888888] hover:text-[#F0F0F0] border-b border-[#1F1F1F] transition-colors duration-150 min-h-[44px] flex items-center"
            >Devlog</a
          >
          <a
            href="/#contact"
            tabindex="-1"
            class="mobile-nav-link py-3 text-sm text-[#888888] hover:text-[#F0F0F0] transition-colors duration-150 min-h-[44px] flex items-center"
            >Contact</a
          >
        </div>
      </div>
    </header>

    <main>
    <!-- ─── HERO ──────────────────────────────────────────────────────────── -->
    <div class="relative pt-14">
      <div
        class="absolute inset-0 overflow-hidden pointer-events-none"
        aria-hidden="true"
      >
        <div
          class="absolute top-1/3 left-1/4 w-[600px] h-[600px] bg-violet-600/5 rounded-full blur-3xl"
        >
        </div>
        <div
          class="absolute top-1/2 right-1/3 w-[300px] h-[300px] bg-[#00FFC8]/3 rounded-full blur-3xl"
        >
        </div>
      </div>

      <div class="relative max-w-5xl mx-auto px-6 py-20">
        <a
          href="/#projects"
          class="hero-enter inline-block text-[#666666] hover:text-[#00FFC8] font-mono text-xs mb-6 tracking-wide transition-colors duration-150"
          style="--delay: 0.05s"
        >
          &larr; Projects
        </a>

        <div
          class="hero-enter flex flex-wrap gap-2 mb-6"
          style="--delay: 0.15s"
        >
          <span
            class="text-xs font-mono bg-cyan-400/[0.08] text-cyan-300/75 border border-cyan-400/20 px-3 py-1 rounded-full"
          >
            In Development
          </span>
          <span
            class="text-xs font-mono bg-cyan-400/[0.08] text-cyan-300/75 border border-cyan-400/20 px-3 py-1 rounded-full"
          >
            Unity 6 · URP
          </span>
        </div>

        <h1
          class="hero-enter font-mono font-bold text-[#F0F0F0] mb-5"
          style="font-size: clamp(2.5rem, 6vw, 4.5rem); line-height: 1.05; letter-spacing: -0.02em; --delay: 0.3s"
        >
          Hide &amp; Seek
        </h1>

        <p
          class="hero-enter text-[#888888] text-lg max-w-2xl leading-relaxed mb-8"
          style="--delay: 0.5s"
        >
          Detection isn't a flag — it's a continuous suspicion float with
          per-state hysteresis, six behaviours implemented as self-contained
          states in a generic <code
            class="font-mono text-[#00FFC8] text-base bg-[#00FFC8]/10 px-1.5 rounded"
            >StateMachine</code
          >, and a static noise event bus that decouples every sound source from
          every listener.
        </p>

        <div class="hero-enter flex flex-wrap gap-3" style="--delay: 0.65s">
          <a
            href="https://github.com/Idzuo32/hide_and_seek_3d"
            target="_blank"
            rel="noopener noreferrer"
            class="github-link inline-flex items-center gap-2 px-5 py-2.5 rounded-lg border border-[#1F1F1F] hover:border-[#888888] text-sm text-[#888888] hover:text-[#F0F0F0] transition-colors duration-150"
          >
            <svg
              class="w-4 h-4"
              fill="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"
              ></path>
            </svg>
            View on GitHub
            <svg
              class="arrow w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
          <a
            href="/devlog/enemy-ai-state-machine"
            class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg border border-[#1F1F1F] hover:border-[#888888] text-sm text-[#888888] hover:text-[#F0F0F0] transition-colors duration-150"
          >
            <svg
              class="w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
              ></path>
            </svg>
            Read the devlog
            <svg
              class="arrow w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- ─── OVERVIEW MEDIA ────────────────────────────────────────────────── -->
    <div class="border-t border-[#1F1F1F]/50 py-12">
      <div class="max-w-4xl mx-auto px-6">
        <div
          class="reveal rounded-xl overflow-hidden border border-[#1F1F1F] bg-[#141414]"
        >
          <video
            class="w-full aspect-video object-cover"
            data-lazy
            autoplay
            loop
            muted
            playsinline
            preload="none"
            poster={poster1.src}
          >
            <source
              src={`${base}assets/thumbnails/hide-and-seek-thumb.mp4`}
              data-src={`${base}assets/thumbnails/hide-and-seek-thumb.mp4`}
              type="video/mp4"
            />
          </video>
        </div>
        <p
          class="reveal text-[#666666] text-xs font-mono mt-3 text-center"
          style="--delay: 100ms"
        >
          Gameplay preview — suspicion build-up, patrol → investigate → chase
          sequence
        </p>
      </div>
    </div>

    <!-- ─── DETECTION AS PRESSURE ──────────────────────────────────────── -->
    <section id="detection" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Making Detection Feel Like Pressure, Not a Trap
        </h2>

        <div
          class="reveal max-w-3xl space-y-5 text-[#888888] leading-relaxed"
          style="--delay: 100ms"
        >
          <p>
            Binary detection — seen or unseen — removes player agency the moment
            it fires. A continuous <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >float</code
            > (0–100) fixes this: the player can see the meter rising, duck
            behind cover, and let it bleed back down before the enemy commits.
            Hysteresis makes that bleed-down reliable — each <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >SeekState</code
            > has separate rising and falling thresholds, so the enemy doesn't
            flicker between states at the boundary. You need to genuinely break
            line of sight to pull the meter back, not just graze the threshold.
          </p>
        </div>
      </div>
    </section>

    <!-- ─── SIX INDEPENDENT BEHAVIOURS ───────────────────────────────────── -->
    <section id="behaviours" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Keeping Six Behaviours Independent of Each Other
        </h2>

        <div
          class="reveal max-w-3xl space-y-5 text-[#888888] leading-relaxed"
          style="--delay: 100ms"
        >
          <p>
            Idle, Patrol, Investigate, Alert, Chase, Search — each state needs
            to read from the suspicion meter, drive the NavMesh, play
            animations, and trigger camera responses. None of those concerns
            belong in a single <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >Update()</code
            > loop. A generic <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >StateMachine</code
            > (plain C# class, no Unity dependency) paired with an <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >IState</code
            > interface makes each state a self-contained class that owns its
            own enter, tick, and exit logic. Adding a state or changing a
            transition touches one file, not the whole controller.
          </p>
        </div>
      </div>
    </section>

    <!-- ─── DECOUPLING NOISE ─────────────────────────────────────────────── -->
    <section id="noise-decoupling" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Decoupling Noise Sources from Listeners
        </h2>

        <div
          class="reveal max-w-3xl space-y-5 text-[#888888] leading-relaxed"
          style="--delay: 100ms"
        >
          <p>
            Anything that makes noise — a sprinting player, a knocked-over
            prop — needs to alert every enemy within range. Neither the emitter
            nor the enemy should hold a reference to the other: the set of
            listening enemies changes at runtime, and emitters shouldn't need to
            know what's listening. A static <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >NoiseEmitter</code
            > event bus with a <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >readonly struct</code
            > payload solves this. Emitters fire-and-forget. Each enemy's <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >NoiseListener</code
            > filters by <code
              class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
              >_hearingRadius</code
            > before raising its own local event. No allocation, no coupling, no
            broadcast-to-all overhead.
          </p>
        </div>
      </div>
    </section>

    <!-- ─── ARCHITECTURE ──────────────────────────────────────────────────── -->
    <section id="architecture" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Architecture
        </h2>

        <div class="space-y-14">
          <!-- Decision 1: IState + StateMachine -->
          <div class="reveal" style="--delay: 0ms">
            <div class="border border-[#1F1F1F] rounded-xl p-8">
              <h3 class="font-mono font-semibold text-[#F0F0F0] text-lg mb-3">
                IState + StateMachine — Plain Classes, Not MonoBehaviours
              </h3>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >StateMachine</code
                > and <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >BaseState</code
                > live in <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Infrastructure/StateMachine/</code
                > with no Unity dependency — they're plain C# classes that could
                run in a test harness without an editor. <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >StateMachine</code
                > holds the current <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >IState</code
                >, calls <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Exit()</code
                > then <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Enter()</code
                > on transition, and exposes <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Tick()</code
                >. <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >EnemyController</code
                > owns one instance, calls <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Tick()</code
                > from <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Update()</code
                >, and constructs each concrete state with a reference to
                itself. The states get everything they need through that owner
                reference — no singletons, no <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >FindObjectOfType</code
                >.
              </p>
              <pre><code class="language-csharp" set:text={`// Infrastructure/Interfaces/IState.cs
public interface IState
{
    void Enter();
    void Tick();
    void Exit();
}

// Infrastructure/StateMachine/StateMachine.cs
public class StateMachine
{
    IState _currentState;
    public IState CurrentState => _currentState;

    public void ChangeState(IState newState)
    {
        _currentState?.Exit();
        _currentState = newState;
        _currentState?.Enter();
    }

    public void Tick() => _currentState?.Tick();
}

// Infrastructure/StateMachine/BaseState.cs
public abstract class BaseState : IState
{
    public virtual void Enter() { }
    public virtual void Tick()  { }
    public virtual void Exit()  { }
}

// AI/Enemy/EnemyController.cs (simplified)
public class EnemyController : MonoBehaviour
{
    StateMachine _stateMachine = new();

    void Start() => _stateMachine.ChangeState(new EnemyPatrolState(this));
    void Update() => _stateMachine.Tick();

    public void ChangeState(IState newState) => _stateMachine.ChangeState(newState);
}`} /></pre>
              <!-- Concrete states -->
              <div
                class="mt-6 p-4 bg-[#0D0D0D] rounded-lg border border-[#1F1F1F]"
              >
                <p class="text-[#666666] text-xs font-mono mb-3">
                  Six concrete states in AI/Enemy/States/
                </p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                  {
                    [
                      ["EnemyIdleState", "Stops navigation, waits a timer, transitions to Patrol"],
                      [
                        "EnemyPatrolState",
                        "Walks a waypoint loop, transitions to Alert or Chase as suspicion rises",
                      ],
                      [
                        "EnemyInvestigateState",
                        "Moves to a specified position, transitions back to Patrol after a look-around",
                      ],
                      [
                        "EnemyAlertState",
                        "Stops and scans toward the last-known position, transitions to Chase or Search",
                      ],
                      [
                        "EnemyChaseState",
                        "Pursues with throttled nav updates, transitions to Search on LoS loss",
                      ],
                      [
                        "EnemySearchState",
                        "Three-phase sweep: LKP → rotate through N directions → nearest patrol waypoints",
                      ],
                    ].map(([name, desc]) => (
                      <div class="flex gap-3 text-xs">
                        <code class="font-mono text-violet-400 shrink-0">
                          {name}
                        </code>
                        <span class="text-[#666666]">{desc}</span>
                      </div>
                    ))
                  }
                </div>
              </div>
            </div>
          </div>

          <!-- Decision 2: SuspicionMeter -->
          <div class="reveal" style="--delay: 50ms">
            <div class="border border-[#1F1F1F] rounded-xl p-8">
              <h3 class="font-mono font-semibold text-[#F0F0F0] text-lg mb-3">
                SuspicionMeter — Continuous Float with Hysteresis
              </h3>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >SuspicionMeter</code
                > is a MonoBehaviour on the enemy that accumulates suspicion each
                <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >FixedUpdate</code
                >. The visual delta from <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >EnemyDetection.ComputeVisualDelta()</code
                > applies three modifiers: distance falloff, angle falloff, and
                a crouch stealth multiplier. The result is a per-frame rate —
                not a spike — so suspicion rises smoothly and the player has
                time to react.
              </p>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >ComputeTargetState()</code
                > uses separate rising and falling thresholds per state. The gap
                between them is the hysteresis band: you need to lose suspicion
                past the falling threshold to revert a state, not just dip below
                the threshold that triggered it. Without that gap, a player
                hovering at the boundary would flicker the enemy between states
                on every frame.
              </p>
              <pre><code class="language-csharp" set:text={`// Detection/SuspicionMeter.cs
public class SuspicionMeter : MonoBehaviour
{
    public event Action<float>            OnSuspicionChanged;
    public event Action<SeekState, SeekState> OnStateChanged;
    public event Action                   OnPlayerCaught;

    public float     Suspicion           { get; private set; }  // 0–100
    public float     SuspicionNormalized => Suspicion / 100f;
    public SeekState State               { get; private set; }
    public float     Phase2SuspicionFloor { get; set; }         // set by LevelPhaseManager

    // Called each FixedUpdate by EnemyDetection
    public void Tick(float visualDeltaPerSec, float audioSpike,
                     bool hasLoS, float proximityDeltaPerSec,
                     bool playerInCatchRadius) { /* ... */ }

    SeekState ComputeTargetState()
    {
        // Rising thresholds (from EnemyData ScriptableObject):
        //   Unaware → Alert:     suspicion >= alertThreshold     (25f)
        //   Alert   → Searching: suspicion >= searchingThreshold (60f)
        //   Searching → Chase:   suspicion >= chaseThreshold     (85f)
        //
        // Falling thresholds (hysteresis gap prevents flickering):
        //   Chase   → Searching: suspicion < chaseRevertThreshold     (75f)
        //   Searching → Alert:   suspicion < searchingRevertThreshold  (50f)
        //   Alert   → Unaware:   suspicion < alertRevertThreshold      (15f)
        // ...
    }
}

// AI/Enemy/EnemyDetection.cs — visual delta per GDD formula F2
float ComputeVisualDelta(float distance, float angleToPlayer)
{
    // distanceFactor: falloff using distanceFalloffExponent (2f) over detectionRange (15f)
    // angleFactor:    falloff using angleFalloffExponent (2f)   over fieldOfViewAngle/2 (45°)
    // crouchModifier: crouchStealthMultiplier (0.4f) when player is crouching
    // stateMultiplier: searchingVisualDetectionMultiplier (1.5f) when Searching
    return data.baseDetectionRate * distanceFactor * angleFactor
               * crouchModifier * stateMultiplier;
}`} /></pre>
            </div>
          </div>

          <!-- Decision 3: EnemyNavigation wrapper + NoiseEmitter bus -->
          <div class="reveal" style="--delay: 100ms">
            <div class="border border-[#1F1F1F] rounded-xl p-8">
              <h3 class="font-mono font-semibold text-[#F0F0F0] text-lg mb-3">
                EnemyNavigation — NavMeshAgent Behind an Interface
              </h3>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                States never touch <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >NavMeshAgent</code
                > directly. <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >EnemyNavigation</code
                > wraps it and exposes only what states need: <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >SetDestination()</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Stop()</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >IsNear()</code
                >, and <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >RotateToward()</code
                >. That last one is the non-obvious method — <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >EnemyAlertState</code
                > uses it to smoothly scan toward the last-known position, and
                it returns <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >true</code
                > when the rotation is within 1° of the target so the state
                knows when to stop waiting. Wrapping the agent means you can
                swap the navigation backend or mock it in tests without touching
                any state logic.
              </p>
              <h3 class="font-mono font-semibold text-[#F0F0F0] text-lg mb-3 mt-8">
                NoiseEmitter — Static Event Bus with Zero Allocation
              </h3>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >NoiseEmitter</code
                > is a static class that fires a <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >readonly struct</code
                > — <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >NoiseEvent</code
                > — carrying world position, intensity, and a source tag.
                Emitters call <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >NoiseEmitter.Emit()</code
                > and forget it. Each enemy's <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >NoiseListener</code
                > subscribes on <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >OnEnable</code
                >, unsubscribes on <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >OnDisable</code
                >, and filters incoming events by <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >_hearingRadius</code
                > before raising its own local event. The struct allocates
                nothing on the heap: no boxing, no list allocation per
                broadcast. Adding a new noise source — a door slam, a
                grenade — means one <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Emit()</code
                > call. Nothing else changes.
              </p>
              <pre><code class="language-csharp" set:text={`// AI/Enemy/EnemyNavigation.cs
[RequireComponent(typeof(NavMeshAgent))]
public class EnemyNavigation : MonoBehaviour
{
    NavMeshAgent _agent;

    public bool IsAtDestination { get; }

    public void SetDestination(Vector3 position) => _agent.SetDestination(position);
    public void SetSpeed(float speed)            => _agent.speed = speed;
    public void Stop()                           => _agent.ResetPath();

    public bool IsNear(Vector3 position, float threshold) =>
        Vector3.Distance(transform.position, position) <= threshold;

    // Used by EnemyAlertState to scan toward last-known-position
    public bool RotateToward(Vector3 target, float degreesPerSecond) { /* returns true when < 1° */ }
}

// Detection/NoiseSystem/NoiseEvent.cs — heap-free event payload
public readonly struct NoiseEvent
{
    public Vector3 WorldPosition { get; init; }
    public float   Intensity     { get; init; }
    public string  SourceTag     { get; init; }
}

// Detection/NoiseSystem/NoiseEmitter.cs — static fire-and-forget bus
public static class NoiseEmitter
{
    public static event Action<NoiseEvent> OnNoiseEmitted;
    public static void Emit(NoiseEvent e) => OnNoiseEmitted?.Invoke(e);
}

// Detection/NoiseSystem/NoiseListener.cs — per-enemy filtered subscriber
public class NoiseListener : MonoBehaviour
{
    [SerializeField] float _hearingRadius;
    public event Action<NoiseEvent> OnNoiseHeard;

    void OnEnable()  => NoiseEmitter.OnNoiseEmitted += HandleNoise;
    void OnDisable() => NoiseEmitter.OnNoiseEmitted -= HandleNoise;

    void HandleNoise(NoiseEvent e)
    {
        if (Vector3.Distance(transform.position, e.WorldPosition) <= _hearingRadius)
            OnNoiseHeard?.Invoke(e);
    }
}`} /></pre>
            </div>
          </div>

          <!-- Decision 4: LevelBuilder editor suite -->
          <div class="reveal" style="--delay: 150ms">
            <div class="border border-[#1F1F1F] rounded-xl p-8">
              <h3 class="font-mono font-semibold text-[#F0F0F0] text-lg mb-3">
                LevelBuilder — Nine Custom Editor Tools
              </h3>
              <p class="text-[#888888] leading-relaxed mb-6 max-w-3xl">
                The <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >Editor/LevelBuilder/</code
                > directory is nine tools designed to hand off to each other
                across the full layout workflow. Placement (<code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >SurfaceDropTool</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >BoundsSnapTool</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >PrefabPaletteWindow</code
                >), alignment and distribution (<code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >BoundsAlignTool</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >DistributeTool</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >ScatterTool</code
                >), and auditing (<code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >OverlapChecker</code
                >, <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >ComponentAuditTool</code
                >) — you scatter props with <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >ScatterTool</code
                >, snap them to geometry with <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >SurfaceDropTool</code
                >, then run <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >OverlapChecker</code
                > to catch collider intersections before hitting play. <code
                  class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                  >LevelBuilderUtility</code
                > provides the shared selection queries, undo helpers, and gizmo
                drawing that all nine tools depend on.
              </p>
              <!-- Tool grid -->
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 mb-6">
                {
                  [
                    [
                      "SurfaceDropTool",
                      "Raycasts selection onto underlying geometry — snaps props to floor/ceiling in one click",
                    ],
                    [
                      "BoundsSnapTool",
                      "Snaps object bounds edges to target surfaces, avoiding gap-between-mesh artefacts",
                    ],
                    [
                      "BoundsAlignTool",
                      "Aligns selection to a reference object's bounds (left/right/top/bottom/front/back)",
                    ],
                    [
                      "DistributeTool",
                      "Distributes selected objects evenly along X, Y, or Z with configurable spacing",
                    ],
                    [
                      "ScatterTool",
                      "Random-placement with overlap avoidance — configurable radius, count, and normal alignment",
                    ],
                    [
                      "OverlapChecker",
                      "Highlights objects whose collider bounds intersect — catches layout errors before play",
                    ],
                    [
                      "PrefabPaletteWindow",
                      "Click-to-place prefab palette with preview — eliminates drag from Project window",
                    ],
                    [
                      "ComponentAuditTool",
                      "Scans scene for missing scripts, empty serialized fields, or incorrect layer assignments",
                    ],
                    [
                      "LevelBuilderUtility",
                      "Shared helpers used by all tools — selection queries, undo helpers, gizmo drawing",
                    ],
                  ].map(([name, desc]) => (
                    <div class="flex flex-col gap-1 p-3 bg-[#0D0D0D] rounded-lg border border-[#1F1F1F]">
                      <code class="font-mono text-violet-400 text-xs">
                        {name}
                      </code>
                      <span class="text-[#666666] text-xs leading-relaxed">
                        {desc}
                      </span>
                    </div>
                  ))
                }
              </div>
              <pre><code class="language-csharp" set:text={`// Editor/LevelBuilder/ — all tools live here as EditorWindow or static utility classes
// Example: SurfaceDropTool drops selected transforms onto the nearest surface below

// Representative pattern (actual implementation details [VERIFY]):
public class SurfaceDropTool : EditorWindow
{
    [MenuItem("Level Builder/Surface Drop Tool")]
    static void Open() => GetWindow<SurfaceDropTool>("Surface Drop");

    void OnGUI()
    {
        if (GUILayout.Button("Drop to Surface"))
        {
            Undo.RecordObjects(Selection.transforms, "Surface Drop");
            foreach (var t in Selection.transforms)
                LevelBuilderUtility.DropToSurface(t);
        }
    }
}`} /></pre>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ─── SCREENSHOTS ───────────────────────────────────────────────────── -->
    <section
      id="screenshots"
      class="py-20 border-t border-[#1F1F1F]/50"
      data-screenshots={`${screenshot1.src},${screenshot2.src}`}
    >
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Screenshots
        </h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div class="reveal" style="--delay: 0ms">
            <div
              class="screenshot-wrap"
              role="button"
              tabindex="0"
              aria-label="Open screenshot 1"
              data-index="0"
            >
              <Image
                src={screenshot1}
                alt="Hide and Seek — top-down gameplay view: patrol route and suspicion meter visible"
                loading="lazy"
                widths={[400, 800, 1200]}
                sizes="(max-width: 640px) 100vw, 50vw"
              />
            </div>
            <p class="text-[#666666] text-xs font-mono mt-2">
              Top-down view — patrol route and suspicion meter visible
            </p>
          </div>

          <div class="reveal" style="--delay: 100ms">
            <div
              class="screenshot-wrap"
              role="button"
              tabindex="0"
              aria-label="Open screenshot 2"
              data-index="1"
            >
              <Image
                src={screenshot2}
                alt="Hide and Seek — enemy chase state: EnemyChaseState active with suspicion meter at maximum"
                loading="lazy"
                widths={[400, 800, 1200]}
                sizes="(max-width: 640px) 100vw, 50vw"
              />
            </div>
            <p class="text-[#666666] text-xs font-mono mt-2">
              Chase state — EnemyChaseState active, suspicion at max
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- ─── RETROSPECTIVE ─────────────────────────────────────────────────── -->
    <section id="retrospective" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          Retrospective
        </h2>

        <ul class="reveal space-y-4 max-w-3xl" style="--delay: 100ms">
          <li class="flex gap-3 text-[#888888] leading-relaxed">
            <span class="text-[#00FFC8] font-mono mt-0.5 shrink-0">—</span>
            <span>
              <strong class="text-[#F0F0F0] font-medium"
                >State transitions allocate.</strong
              >
              Each <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >ChangeState(new EnemyChaseState(this))</code
              > creates a new heap object. With six states and frequent transitions,
              this generates measurable GC. Pre-allocating all states at <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >Awake()</code
              > and passing them by reference would eliminate that allocation entirely.
            </span>
          </li>
          <li class="flex gap-3 text-[#888888] leading-relaxed">
            <span class="text-[#00FFC8] font-mono mt-0.5 shrink-0">—</span>
            <span>
              <strong class="text-[#F0F0F0] font-medium"
                >Phase 2 escalation is three loose properties, not a profile.</strong
              >
              <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >LevelPhaseManager</code
              > escalates the enemy by setting <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >Phase2SpeedMultiplier</code
              >, <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >Phase2SuspicionFloor</code
              >, and <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >Phase2SkipAlertScan</code
              > directly on <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >EnemyController</code
              >. There's already an <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >EscalationProfile</code
              > ScriptableObject in the <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >Data/</code
              > folder — it should drive all phase transitions instead of the three
              ad-hoc setters.
            </span>
          </li>
          <li class="flex gap-3 text-[#888888] leading-relaxed">
            <span class="text-[#00FFC8] font-mono mt-0.5 shrink-0">—</span>
            <span>
              <strong class="text-[#F0F0F0] font-medium"
                >SuspicionMeter has four separate timer floats.</strong
              >
              <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >_detectionCooldownTimer</code
              >, <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >_chaseNoInputTimer</code
              >, <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >_catchDwellTimer</code
              >, and <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >_caughtFired</code
              > are all managed inline. A small reusable <code
                class="font-mono text-[#00FFC8] text-sm bg-[#00FFC8]/10 px-1 rounded"
                >CountdownTimer</code
              > struct that ticks and fires a callback would cut this down significantly
              and make the intent explicit.
            </span>
          </li>
        </ul>
      </div>
    </section>

    <!-- ─── WHAT'S NEXT ───────────────────────────────────────────────────── -->
    <section id="next" class="py-20 border-t border-[#1F1F1F]/50">
      <div class="max-w-5xl mx-auto px-6">
        <h2 class="reveal text-2xl font-bold text-[#F0F0F0] mb-10">
          What's Next
        </h2>

        <ul class="reveal space-y-3 max-w-3xl" style="--delay: 100ms">
          {
            [
              "<strong class='text-[#F0F0F0] font-medium'>SFX pass on the noise system.</strong> Footstep intensity varies by stance — walk, sprint, crouch — via PlayerNoiseEmitter. The wiring is in place; it needs audio assets and an alert sting on SeekState transitions.",
              "<strong class='text-[#F0F0F0] font-medium'>Hiding spots.</strong> The wardrobe interaction is wired up. What's missing is the tension layer: a slow creeping suspicion drain rate while inside — distinct from normal falloff — and a VFX pass to sell the dread.",
              "<strong class='text-[#F0F0F0] font-medium'>Win condition.</strong> RoundTimer expiry already calls WinConditionEvaluator. The evaluation logic is done; the win screen and score display aren't.",
              "<strong class='text-[#F0F0F0] font-medium'>Phase 2 escalation.</strong> LevelPhaseManager and the EscalationProfile ScriptableObject both exist. The remaining work is connecting the profile to the game loop through the LevelPhase enum so phase transitions are data-driven rather than the three ad-hoc setters called out in the Retrospective.",
              "<strong class='text-[#F0F0F0] font-medium'>More level layouts.</strong> The LevelBuilder toolset makes iteration low-effort — scatter, drop to surface, audit. The constraint is art assets, not tooling.",
            ].map((item) => (
              <li class="flex gap-3 text-[#888888]">
                <span class="text-violet-400 font-mono shrink-0">›</span>
                <span set:html={item} />
              </li>
            ))
          }
        </ul>
      </div>
    </section>

    </main>

    <!-- ─── FOOTER ────────────────────────────────────────────────────────── -->
    <footer class="border-t border-[#1F1F1F]/50 py-8">
      <div
        class="max-w-5xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4"
      >
        <p class="text-[#888888] text-sm">© 2026 Marko Stankovic</p>
        <div class="flex items-center gap-6 text-sm">
          <a
            href="/projects/endless-runner"
            class="nav-link text-[#888888] hover:text-[#F0F0F0] transition-colors duration-150"
          >
            Endless Runner →
          </a>
        </div>
        <p class="text-[#666666] text-xs font-mono">Game Developer · Serbia</p>
      </div>
    </footer>

    <!-- ─── LIGHTBOX ──────────────────────────────────────────────────────── -->
    <div
      id="lightbox"
      role="dialog"
      aria-modal="true"
      aria-label="Screenshot viewer"
      class="fixed inset-0 z-[200] items-center justify-center"
    >
      <div id="lightbox-backdrop" class="absolute inset-0 bg-[#0D0D0D]/95">
      </div>
      <div
        id="lightbox-content"
        class="relative z-10 flex items-center gap-3 px-4"
      >
        <button
          id="lightbox-prev"
          class="lb-btn"
          aria-label="Previous screenshot"
        >
          <svg
            width="20"
            height="20"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            viewBox="0 0 24 24"
            aria-hidden="true"
            ><path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M15 19l-7-7 7-7"></path></svg
          >
        </button>
        <img id="lightbox-img" src="data:," alt="Project screenshot" />
        <button id="lightbox-next" class="lb-btn" aria-label="Next screenshot">
          <svg
            width="20"
            height="20"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            viewBox="0 0 24 24"
            aria-hidden="true"
            ><path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M9 5l7 7-7 7"></path></svg
          >
        </button>
      </div>
      <button
        id="lightbox-close"
        class="lb-btn z-10"
        aria-label="Close lightbox"
      >
        <svg
          width="20"
          height="20"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
          aria-hidden="true"
          ><path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M6 18L18 6M6 6l12 12"></path></svg
        >
      </button>
    </div>

    <script>
      // Syntax highlighting — called directly since module scripts run after DOM is parsed
      // @ts-ignore
      if (typeof hljs !== "undefined") hljs.highlightAll();

      // Wrap pre blocks in .pre-wrap and add copy button
      document.querySelectorAll("pre").forEach((pre) => {
        const wrap = document.createElement("div");
        wrap.className = "pre-wrap";
        pre.parentNode?.insertBefore(wrap, pre);
        wrap.appendChild(pre);

        const btn = document.createElement("button");
        btn.className = "copy-btn";
        btn.textContent = "copy";
        btn.setAttribute("aria-label", "Copy code");
        wrap.appendChild(btn);

        btn.addEventListener("click", () => {
          const code = pre.querySelector("code");
          const text = code?.textContent ?? "";
          navigator.clipboard.writeText(text).then(() => {
            btn.textContent = "copied!";
            btn.classList.add("copied");
            setTimeout(() => {
              btn.textContent = "copy";
              btn.classList.remove("copied");
            }, 2000);
          });
        });
      });

      const prefersReduced = window.matchMedia(
        "(prefers-reduced-motion: reduce)",
      ).matches;
      const revealEls = document.querySelectorAll(".reveal");
      if (!prefersReduced) {
        const obs = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) {
                e.target.classList.add("visible");
                obs.unobserve(e.target);
              }
            });
          },
          { threshold: 0.1 },
        );
        revealEls.forEach((el) => obs.observe(el));
      } else {
        revealEls.forEach((el) => el.classList.add("visible"));
      }

      // Lazy-load gameplay video when it enters the viewport
      const lazyVideo = document.querySelector<HTMLVideoElement>("video[data-lazy]");
      if (lazyVideo) {
        const videoObs = new IntersectionObserver(
          (entries) => {
            if (entries[0].isIntersecting) {
              const source = lazyVideo.querySelector<HTMLSourceElement>("source[data-src]");
              if (source) {
                source.src = source.dataset.src!;
                lazyVideo.load();
                lazyVideo.play().catch(() => {});
              }
              videoObs.unobserve(lazyVideo);
            }
          },
          { rootMargin: "200px" },
        );
        videoObs.observe(lazyVideo);
      }

      const menuBtn = document.getElementById("menu-btn");
      const mobileNav = document.getElementById("mobile-nav");
      const iconMenu = document.getElementById("icon-menu");
      const iconClose = document.getElementById("icon-close");
      const mobileNavLinks = document.querySelectorAll<HTMLElement>(".mobile-nav-link");

      function closeMobileNav() {
        mobileNav?.classList.remove("open");
        mobileNav?.setAttribute("aria-hidden", "true");
        menuBtn?.setAttribute("aria-expanded", "false");
        iconMenu?.classList.remove("hidden");
        iconClose?.classList.add("hidden");
        mobileNavLinks.forEach((link) => link.setAttribute("tabindex", "-1"));
      }

      menuBtn?.addEventListener("click", () => {
        const isOpen = mobileNav?.classList.toggle("open");
        menuBtn.setAttribute("aria-expanded", String(isOpen));
        iconMenu?.classList.toggle("hidden", !!isOpen);
        iconClose?.classList.toggle("hidden", !isOpen);
        if (isOpen) {
          mobileNav?.removeAttribute("aria-hidden");
          mobileNavLinks.forEach((link) => link.removeAttribute("tabindex"));
        } else {
          mobileNav?.setAttribute("aria-hidden", "true");
          mobileNavLinks.forEach((link) => link.setAttribute("tabindex", "-1"));
        }
      });
      mobileNavLinks.forEach((link) => {
        link.addEventListener("click", closeMobileNav);
      });

      const lightbox = document.getElementById("lightbox");
      const lbImg = document.getElementById("lightbox-img");
      const lbPrev = document.getElementById("lightbox-prev");
      const lbNext = document.getElementById("lightbox-next");
      const lbClose = document.getElementById("lightbox-close");
      const lbBackdrop = document.getElementById("lightbox-backdrop");
      const section = document.getElementById("screenshots");
      const lbScreenshots = (section?.dataset.screenshots ?? "")
        .split(",")
        .map((s) => s.trim())
        .filter(Boolean);
      let lbIndex = 0;
      let lbPrevFocus = null;

      function lbShow(index) {
        lbIndex = index;
        lbPrevFocus = document.activeElement;
        lbUpdate();
        lightbox.style.display = "flex";
        requestAnimationFrame(() => {
          lightbox.classList.add("lb-open");
          document.body.style.overflow = "hidden";
          lbClose.focus();
        });
      }
      function lbHide() {
        lightbox.classList.remove("lb-open");
        lightbox.addEventListener(
          "transitionend",
          () => {
            lightbox.style.display = "none";
            document.body.style.overflow = "";
            lbPrevFocus?.focus();
          },
          { once: true },
        );
      }
      function lbUpdate() {
        lbImg.src = lbScreenshots[lbIndex];
        lbImg.alt = `Screenshot ${lbIndex + 1} of ${lbScreenshots.length}`;
        lbPrev.disabled = lbIndex === 0;
        lbNext.disabled = lbIndex === lbScreenshots.length - 1;
      }

      document.querySelectorAll(".screenshot-wrap").forEach((wrap) => {
        const open = () => lbShow(parseInt(wrap.dataset.index ?? "0", 10));
        wrap.addEventListener("click", open);
        wrap.addEventListener("keydown", (e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            open();
          }
        });
      });
      lbClose.addEventListener("click", lbHide);
      lbBackdrop.addEventListener("click", lbHide);
      lbPrev.addEventListener("click", () => {
        if (lbIndex > 0) {
          lbIndex--;
          lbUpdate();
        }
      });
      lbNext.addEventListener("click", () => {
        if (lbIndex < lbScreenshots.length - 1) {
          lbIndex++;
          lbUpdate();
        }
      });
      lightbox.addEventListener("keydown", (e) => {
        if (e.key === "Escape") lbHide();
        else if (e.key === "ArrowLeft" && lbIndex > 0) {
          lbIndex--;
          lbUpdate();
        } else if (
          e.key === "ArrowRight" &&
          lbIndex < lbScreenshots.length - 1
        ) {
          lbIndex++;
          lbUpdate();
        }
      });
    </script>
</BaseLayout>
