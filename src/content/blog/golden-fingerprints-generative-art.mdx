---
title: "Golden Fingerprints: Regression Tests for Generative Art"
date: "2026-07-07"
excerpt: "A refactor can shift one gradient band by a pixel and no test notices, until it ships. I regression-test my generative engines by committing SHA-256 hashes of the raw pixel and sample arrays. Here is the harness, and the rules that keep it from lying."
opener: "My media engines render app art, music beds and wallpapers from seeded math, and a refactor can move one gradient band by a pixel without a single test noticing. So the bytes became the test: SHA-256 fingerprints of the raw arrays, committed beside each engine. This entry is the harness, and the three rules that keep a golden from lying."
tags: ["Testing", "Generative Media", "Python", "NumPy", "Determinism"]
beat: engine
ogImage: "/images/blog/golden-fingerprints-generative-art/og-golden-fingerprints.png"
draft: false
entry: "01"
---

The renders come from seeded math (procedural, no trained model anywhere) and ship baked into app builds, so a shifted band is a store release to fix: a refactor that nudges a gradient stop, a "harmless" cleanup that reorders RNG draws. Unit tests on the math do not catch this class. The output is an image or a waveform, and the question is whether the bytes changed.

So the bytes are the test. I call them golden fingerprints.

## Hash the array, not the file

The harness lives in one small module, `goldens.py`. A fingerprint is three fields: the array's shape, its dtype, and the SHA-256 of its raw bytes. The hash is taken on the ndarray before any encoding, deliberately. PNG bytes vary across libpng versions; pixels don't. Hashing the render itself makes the fingerprint portable across image libraries.

![One render at the pinned seed gives an ndarray of shape 2400 by 1080 by 3, dtype uint8. Two paths lead down from it. The raw ndarray path takes the bytes with tobytes and hashes them with sha256, giving a fingerprint of three fields, shape, dtype and sha256, and the same digest on every machine. The PNG file path encodes through libpng into file bytes, and the same pixels can give other bytes on another libpng, so the digest can move with no pixel change and the file is not a fingerprint.](/images/blog/golden-fingerprints-generative-art/hash-the-array.svg)

A reference file per engine commits the fingerprints. The music engine pins all six moods at seed 30. The journey-biome engine pins six biomes at seed 42, plus the full stacked canvas and two sea variants. Landscape pins six palettes, aurora pins five palettes at two timepoints each, and the progress panorama pins all twelve of its tiles. A test renders, hashes, and compares. Any drift in geometry, palette, grain, or RNG consumption fails loudly with the exact case name.

![A table with one row per reference file. Journey biome: seed 42, 9 cases, uint8, six biomes plus the stacked canvas and two sea variants. Landscape: seed 42, 7 cases, uint8, six palettes at t 0 plus forest at t 7.5. Aurora: no seed, 10 cases, uint8, five palettes at t 0 and t 20 with meteors off. Progress panorama: seed 52, 12 cases, uint8, six tiles each in colour and gray. Music: seed 30, 6 cases, float64, six moods of 8 bars each. Totals: 44 pinned cases, 38 tests in run-goldens.sh across 11 classes. The music row is outlined in amber with a note that float64 is toolchain locked, so a numpy, BLAS or FFT change can trip a false regression.](/images/blog/golden-fingerprints-generative-art/reference-table.svg)

`run-goldens.sh` runs the whole lane in about a minute, 38 tests, because it hashes raw render output and never touches ffmpeg. Pulling in the encode-heavy suites would turn a fast pixel-pin into a multi-minute run. It also runs a JS conformance suite, which belongs to the look-dev workflow in the [prototype in the browser, ship in Python](/field-journal/browser-prototype-then-port/) entry.

## The rules that keep the goldens honest

A golden test is easy to build and easy to ruin. Three rules came out of building this one.

**Updating a reference is per-engine and explicit.** Each engine has its own environment variable: `UPDATE_MUSIC_REF=1` regenerates the music reference and nothing else. I rejected a single global `UPDATE_REF` flag on purpose. Run under test discovery, a global flag would re-pin every engine at once, including the one you just broke. The blast radius of "I meant to update one reference" should be one reference.

![Two runs compared across five reference files, one row per engine with its flag: music with UPDATE_MUSIC_REF, journey biome marked broken with UPDATE_JOURNEY_REF, landscape with UPDATE_LANDSCAPE_REF, aurora with UPDATE_AURORA_REF, progress panorama with UPDATE_PANORAMA_REF. Run A sets UPDATE_MUSIC_REF=1 and runs python3 -m unittest test_music: only the music row is pinned and the other four stay untouched, one reference re-pinned. Run B is the rejected hypothetical: a single UPDATE_REF=1 under unittest discover pins all five rows at once, and the broken journey biome engine is pinned as correct, so nothing trips.](/images/blog/golden-fingerprints-generative-art/update-blast-radius.svg)

**The pin never precedes the eyeball.** A fingerprint asserts "the output has not changed since a human approved it." That makes human approval the load-bearing step. When I ported the sea biome's new look into the engine, the tests for the math went green and the golden stayed intentionally red, because the render hadn't been reviewed on an actual phone yet. Pinning first would have enshrined an unreviewed image as correct forever.

**Verify the test can fail.** Before trusting the guard, I broke an engine on purpose and watched the fingerprint trip. A regression test that has never been red is a hope. This cost five minutes and caught a case where a skipped-reference path made a whole suite silently pass.

![Two result rows. The first, marked PASS in green, reads "render at seed 42, untouched: sha256 matches the committed reference". The second, marked FAIL in red, reads "same render, one pixel's low bit flipped" and shows an expected hash beginning dbf8ca0c against a different hash beginning 856b11cc. Below: the array is 2400 by 1080 by 3, so that is one changed value out of 7,776,000.](/images/blog/golden-fingerprints-generative-art/golden-fingerprint-trip.svg)

That is the whole contract in two rows. One flipped bit in one channel of one pixel, out of nearly eight million values, and the guard trips with the case name attached.

## The toolchain is part of the fingerprint

The caveat: the visual engines output uint8, which is stable everywhere. The music engine outputs float64, and float64 is only bit-exact on one toolchain. A different numpy, BLAS, or FFT backend can flip low-order bits and trip a false regression while the audio is identical to any ear.

The fix is to treat the toolchain as pinned along with the reference: refs are generated on one machine, numpy gets pinned before the suite runs anywhere else, and an intentional change regenerates refs on that same machine. The comment at the top of the script says exactly this, because future me will forget.

## What this buys

The engines are now refactorable. I rewrote the biome seam model, restructured band fills, and added new edge types, all against a wall of fingerprints that would have caught any accidental change to shipped art. For deterministic generative work this is the cheapest safety net I know, and a human eyeball stays the only thing allowed to bless a new reference.

`goldens.py` is 77 lines. If you pin renders and want it, write me: contact@markostankovic.org.
